
/**
 * Default transition.
 */

const defaultTransition = '0.5s ease';

/**
 * Fast transition.
 */

const fastTransition = '0.2s ease';

/**
 * Quick smooth transition.
 */

const quickSmoothTransition = '0.2s cubic-bezier(.07, .88, .81, .6)';

/**
 * Export `animations`.
 */

export const animations = {
  defaultTransition,
  fastTransition,
  quickSmoothTransition
};
