
/**
 * Module dependencies.
 */

import { animations } from './animations';
import { colors } from './colors';

/**
 * Export `theme` config.
 */

export const theme = {
  animations,
  colors,
  grid: {
    gutter: 40,
    gutterMobile: 16
  }
};
