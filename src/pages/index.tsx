
/**
 * Module dependencies.
 */

import React, { ReactElement } from 'react';

/**
 * `Home` page.
 */

const Home = (): ReactElement => (
  <>
    {'Onda website'}
  </>
);

/**
 * Export `Home` page.
 */

export default Home;
