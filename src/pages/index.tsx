
/**
 * Module dependencies.
 */

import React, { ReactElement } from 'react';

/**
 * `Home` page.
 */

const Home = (): ReactElement => (
  <>
    {'Filecoin retrieval market website'}
  </>
);

/**
 * Export `Home` page.
 */

export default Home;
